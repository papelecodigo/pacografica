import { jsPDF } from 'https://esm.sh/jspdf@2.5.2';
import {supabase,db} from './erp-db.js';

const $=id=>document.getElementById(id);
const $$=sel=>[...document.querySelectorAll(sel)];
const brl=v=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(Number(v||0));
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const localDate=()=>new Intl.DateTimeFormat('en-CA',{timeZone:'America/Sao_Paulo'}).format(new Date());
const META='[[PACO_V6]]';
let wrapped=false;
let lastEnhancedSale=null;
let graphTimer=null;

function injectCss(){if(document.querySelector('link[data-sale-flow-v6]'))return;const l=document.createElement('link');l.rel='stylesheet';l.href='./sale-flow-v6.css?build=20260831-2054';l.dataset.saleFlowV6='1';document.head.appendChild(l)}
function toast(msg,error=false){const e=$('toast');if(!e)return;e.textContent=msg;e.className=`toast show${error?' error':''}`;clearTimeout(window.__saleV6Toast);window.__saleV6Toast=setTimeout(()=>e.className='toast',3400)}
function numberFromMoney(text){return Number(String(text||'').replace(/[^\d,-]/g,'').replace(/\./g,'').replace(',','.'))||0}
function getTotal(){return numberFromMoney($('saleTotal')?.textContent)}
function parseMeta(note=''){const s=String(note||''),i=s.indexOf(META);if(i<0)return{note:s.trim()};try{return{...JSON.parse(s.slice(i+META.length)),note:s.slice(0,i).trim()}}catch{return{note:s.trim()}}}
function encodedNote(meta){const note=$('saleNote')?.value.trim()||'';return`${note}${note?'\n':''}${META}${JSON.stringify(meta)}`}
function currentUser(){return supabase.auth.getSession().then(x=>x.data.session?.user||null)}

async function companySettings(){const user=await currentUser();if(!user)return{};const{data}=await supabase.from('company_settings').select('*').eq('user_id',user.id).maybeSingle();return data||{}}
function companyAddress(s){return[s.address,s.city&&s.state?`${s.city}/${s.state}`:s.city||s.state,s.zip_code&&`CEP ${s.zip_code}`].filter(Boolean).join(' · ')||'Endereço da gráfica ainda não configurado.'}

async function injectSaleFields(){
  const section=$('section-sale');if(!section||$('saleLogisticsPanel'))return;
  const stack=section.querySelector('.stack'),first=stack?.querySelector('.panel');if(!stack||!first)return;
  const settings=await companySettings();
  first.querySelector('.section-title p')?.replaceChildren(document.createTextNode('Cliente, responsável e contato.'));
  const note=$('saleNote');if(note){note.placeholder='Acabamento, arte, detalhes e observações...';note.closest('label')?.querySelector?.('span')}
  first.insertAdjacentHTML('afterend',`
    <article id="saleLogisticsPanel" class="panel sale-logistics-panel">
      <div class="section-title"><b>02</b><div><h3>Prazo e recebimento</h3><p>Quando fica pronto e como o cliente recebe.</p></div></div>
      <div class="form-grid four sale-logistics-grid">
        <label>Prazo<input id="saleDueDate" type="date"></label>
        <label>Horário<input id="saleDueTime" type="time" value="17:00"></label>
        <label>Recebimento<select id="saleFulfillment"><option value="pickup">Retirada na gráfica</option><option value="delivery">Entrega</option></select></label>
        <label>Condição<select id="salePaymentCondition"><option value="avista">À vista</option><option value="entrada_retirada">Entrada + saldo na retirada</option><option value="entrada_entrega">Entrada + saldo na entrega</option><option value="retirada">Pagar na retirada</option><option value="entrega">Pagar na entrega</option></select></label>
      </div>
      <div id="salePickupInfo" class="sale-pickup-info"><span>Local de retirada</span><b>${esc(settings.trade_name||'Papel e Código')}</b><small>${esc(companyAddress(settings))}</small></div>
      <div id="saleDeliveryFields" class="sale-delivery-fields hidden">
        <div class="form-grid three"><label>Endereço<input id="saleDeliveryAddress" placeholder="Rua / Avenida"></label><label>Número<input id="saleDeliveryNumber"></label><label>Complemento<input id="saleDeliveryComplement"></label></div>
        <div class="form-grid four"><label>Bairro<input id="saleDeliveryDistrict"></label><label>Cidade<input id="saleDeliveryCity"></label><label>UF<input id="saleDeliveryState" maxlength="2"></label><label>CEP<input id="saleDeliveryZip"></label></div>
        <label>Taxa de entrega<input id="saleDeliveryFee" type="number" min="0" step="0.01" value="0"></label>
      </div>
    </article>`);
  const itemPanel=stack.querySelectorAll('.panel')[2];itemPanel?.querySelector('.section-title b')?.replaceChildren(document.createTextNode('03'));
  const checkout=section.querySelector('.checkout-card');
  const paymentLabel=$('salePayment')?.closest('label');
  paymentLabel?.insertAdjacentHTML('afterend',`<div id="saleEntryBlock" class="sale-entry-block"><label>Entrada<input id="saleEntry" type="number" min="0" step="0.01" value="0"></label><div class="sale-balance-preview"><span>Saldo restante</span><strong id="saleRemaining">R$ 0,00</strong><small id="saleBalanceHint">Pagamento à vista</small></div></div>`);
  checkout?.classList.add('sale-checkout-v6');
  $('saleFulfillment').onchange=syncFulfillment;
  $('salePaymentCondition').onchange=syncPayment;
  $('saleEntry').oninput=syncPayment;
  section.addEventListener('input',e=>{if(e.target.closest('.sale-item')||e.target.id==='saleDiscount'||e.target.id==='saleDeliveryFee')setTimeout(syncPayment,0)});
  syncFulfillment();syncPayment();
}

function syncFulfillment(){const delivery=$('saleFulfillment')?.value==='delivery';$('saleDeliveryFields')?.classList.toggle('hidden',!delivery);$('salePickupInfo')?.classList.toggle('hidden',delivery);if(delivery&&$('salePaymentCondition')?.value==='entrada_retirada')$('salePaymentCondition').value='entrada_entrega';if(!delivery&&$('salePaymentCondition')?.value==='entrada_entrega')$('salePaymentCondition').value='entrada_retirada';syncPayment()}
function syncPayment(){if(!$('saleEntry'))return;const total=getTotal()+Number($('saleDeliveryFee')?.value||0),cond=$('salePaymentCondition')?.value||'avista',entry=$('saleEntry');let value=Number(entry.value||0);if(cond==='avista'){value=total;entry.value=total.toFixed(2);entry.readOnly=true}else if(['retirada','entrega'].includes(cond)){value=0;entry.value='0';entry.readOnly=true}else{entry.readOnly=false;value=Math.min(total,Math.max(0,value));if(Number(entry.value)!==value)entry.value=value.toFixed(2)}const remaining=Math.max(0,total-value);$('saleRemaining').textContent=brl(remaining);const names={avista:'Pagamento à vista',entrada_retirada:'Saldo na retirada',entrada_entrega:'Saldo na entrega',retirada:'Pagamento integral na retirada',entrega:'Pagamento integral na entrega'};$('saleBalanceHint').textContent=names[cond]||''}

function snapshotMeta(){const total=getTotal(),fee=Number($('saleDeliveryFee')?.value||0),grandTotal=total+fee,cond=$('salePaymentCondition')?.value||'avista',entry=cond==='avista'?grandTotal:Number($('saleEntry')?.value||0),fulfillment=$('saleFulfillment')?.value||'pickup';return{version:6,due_date:$('saleDueDate')?.value||null,due_time:$('saleDueTime')?.value||null,fulfillment,payment_condition:cond,entry_amount:Math.min(grandTotal,Math.max(0,entry)),remaining_amount:Math.max(0,grandTotal-entry),delivery_fee:fee,delivery:fulfillment==='delivery'?{address:$('saleDeliveryAddress')?.value.trim()||'',number:$('saleDeliveryNumber')?.value.trim()||'',complement:$('saleDeliveryComplement')?.value.trim()||'',district:$('saleDeliveryDistrict')?.value.trim()||'',city:$('saleDeliveryCity')?.value.trim()||'',state:$('saleDeliveryState')?.value.trim().toUpperCase()||'',zip:$('saleDeliveryZip')?.value.trim()||''}:null}}
function validateSaleExtra(){if(!$('saleDueDate')?.value){$('saleDueDate').focus();toast('Escolha a data prevista para o material.',true);return false}if($('saleFulfillment')?.value==='delivery'&&!$('saleDeliveryAddress')?.value.trim()){ $('saleDeliveryAddress').focus();toast('Informe o endereço de entrega.',true);return false}return true}

async function latestSale(){const user=await currentUser();if(!user)return null;const{data,error}=await supabase.from('sales').select('*,sale_items(*)').eq('user_id',user.id).order('created_at',{ascending:false}).limit(1).maybeSingle();if(error)throw error;return data}
function normalizedPhone(v){return String(v||'').replace(/\D/g,'')}
async function ensureCustomer(sale,meta){if(!sale.customer_name)return null;const user=await currentUser();if(!user)return null;db.setUser(user);await db.detectERP();const customers=await db.list('customers'),phone=normalizedPhone(sale.customer_phone),name=String(sale.customer_name).trim();let c=customers.find(x=>String(x.name||'').trim().toLowerCase()===name.toLowerCase()||(phone&&[x.phone,x.whatsapp].map(normalizedPhone).includes(phone)));const delivery=meta.delivery||{};const payload={name,whatsapp:sale.customer_phone||null,seller_name:sale.seller_name||null,source:'Venda rápida',active:true,address:delivery.address?[delivery.address,delivery.number,delivery.complement].filter(Boolean).join(', '):c?.address||null,city:delivery.city||c?.city||null,state:delivery.state||c?.state||null,zip_code:delivery.zip||c?.zip_code||null};if(!c)c=await db.insert('customers',payload);else{const patch={};for(const[k,v]of Object.entries(payload))if(v&&!c[k])patch[k]=v;if(Object.keys(patch).length)c=await db.update('customers',c.id,patch)}return c}
async function ensureLead(sale,meta){if(!sale.customer_name)return;const user=await currentUser();if(!user)return;const marker=`[VENDA:${sale.id}]`;const{data:existing}=await supabase.from('leads').select('id').eq('user_id',user.id).ilike('note',`%${marker}%`).limit(1);if(existing?.length)return;const interest=(sale.sale_items||[]).map(i=>i.description).filter(Boolean).slice(0,3).join(' + ')||'Venda rápida';const note=[marker,'Criado automaticamente ao concluir a venda.',meta.note].filter(Boolean).join('\n');await supabase.from('leads').insert({user_id:user.id,customer_name:sale.customer_name,customer_phone:sale.customer_phone||null,service_interest:interest,estimated_value:Number(sale.total||0)+Number(meta.delivery_fee||0),seller_name:sale.seller_name||null,stage:'aprovado',note})}
async function ensureFinance(sale,customer,meta){const user=await currentUser();if(!user)return;db.setUser(user);await db.detectERP();const grand=Number(sale.total||0)+Number(meta.delivery_fee||0),entry=Math.min(grand,Number(meta.entry_amount||0)),remaining=Math.max(0,grand-entry),key=`Venda ${String(sale.id).slice(0,8).toUpperCase()}`;const recs=await db.list('receivables');if(!recs.some(r=>String(r.description||'').includes(key)))await db.insert('receivables',{customer_id:customer?.id||null,description:`${key} · ${sale.customer_name||'Balcão'}`,due_date:meta.due_date||localDate(),amount:grand,received_amount:entry,status:remaining<=0?'paid':entry>0?'partial':'pending',payment_method:sale.payment_method||null});if(entry>0){const{data:exists}=await supabase.from('cash_movements').select('id').eq('user_id',user.id).eq('description',`${key} · entrada`).limit(1);if(!exists?.length)await supabase.from('cash_movements').insert({user_id:user.id,type:'entrada',amount:entry,category:'Venda',description:`${key} · entrada`,nature:'operational'})}}

async function postProcessSale(meta){try{const sale=await latestSale();if(!sale)return;lastEnhancedSale={...sale,_meta:{...meta,note:parseMeta(sale.note).note}};const doneKey=`paco_v6_sale_${sale.id}`;if(localStorage.getItem(doneKey)==='1')return;const customer=await ensureCustomer(sale,lastEnhancedSale._meta);await ensureLead(sale,lastEnhancedSale._meta);await ensureFinance(sale,customer,lastEnhancedSale._meta);localStorage.setItem(doneKey,'1');$('receiptTitle').textContent=`Venda registrada · ${meta.remaining_amount>0?`saldo ${brl(meta.remaining_amount)}`:'paga'}`;const text=$('receiptDialog')?.querySelector('.receipt-success p');if(text)text.textContent=meta.remaining_amount>0?`Entrada ${brl(meta.entry_amount)} · restante ${brl(meta.remaining_amount)}.`:'Pagamento registrado integralmente.';$('refreshBtn')?.click();setTimeout(renderFinanceGraph,500)}catch(e){console.error('Pós-venda V6',e);toast('Venda salva, mas houve falha ao sincronizar CRM/financeiro.',true)}}

function wrapFinish(){const btn=$('finishSaleBtn');if(!btn||btn.dataset.saleV6==='1'||typeof btn.onclick!=='function')return false;const original=btn.onclick;btn.dataset.saleV6='1';btn.onclick=async function(e){if(!validateSaleExtra())return;syncPayment();const meta=snapshotMeta(),note=$('saleNote');if(note)note.value=encodedNote(meta);await original.call(this,e);if($('receiptDialog')?.open)await postProcessSale(meta)};wrapped=true;return true}

function paymentConditionName(v){return({avista:'À vista',entrada_retirada:'Entrada + saldo na retirada',entrada_entrega:'Entrada + saldo na entrega',retirada:'Pagamento na retirada',entrega:'Pagamento na entrega'})[v]||v||'Não informado'}
function fulfillmentName(v){return v==='delivery'?'Entrega':'Retirada na gráfica'}
function deliveryAddress(meta,settings){if(meta.fulfillment!=='delivery')return companyAddress(settings);const d=meta.delivery||{};return[[d.address,d.number].filter(Boolean).join(', '),d.complement,d.district,[d.city,d.state].filter(Boolean).join('/'),d.zip&&`CEP ${d.zip}`].filter(Boolean).join(' · ')||'Endereço de entrega não informado'}
function addText(doc,text,x,y,max=178,opts={}){const lines=doc.splitTextToSize(String(text||''),max);doc.text(lines,x,y,opts);return y+lines.length*5}
async function buildEnhancedReceipt(sale){const meta={...parseMeta(sale.note)},settings=await companySettings(),doc=new jsPDF({unit:'mm',format:'a4'});const grand=Number(sale.total||0)+Number(meta.delivery_fee||0),entry=meta.entry_amount??(meta.payment_condition==='avista'?grand:0),remaining=Math.max(0,meta.remaining_amount??grand-entry);doc.setFillColor(8,23,43);doc.rect(0,0,210,38,'F');doc.setTextColor(255,255,255);doc.setFont('helvetica','bold');doc.setFontSize(17);doc.text(settings.trade_name||'Papel e Código',16,15);doc.setFontSize(8);doc.setFont('helvetica','normal');const head=[settings.legal_name,settings.cnpj&&`CNPJ ${settings.cnpj}`,settings.state_registration&&`IE ${settings.state_registration}`].filter(Boolean).join(' · ');if(head)doc.text(head,16,22);doc.setTextColor(223,240,31);doc.setFont('helvetica','bold');doc.text('RECIBO / COMPROVANTE DE PEDIDO',16,30);doc.setTextColor(22,34,50);let y=49;doc.setFontSize(9);doc.setFont('helvetica','bold');doc.text(`Venda ${String(sale.id).slice(0,8).toUpperCase()}`,16,y);doc.setFont('helvetica','normal');doc.text(new Intl.DateTimeFormat('pt-BR',{dateStyle:'short',timeStyle:'short',timeZone:'America/Sao_Paulo'}).format(new Date(sale.created_at)),194,y,{align:'right'});y+=9;doc.setDrawColor(224,230,238);doc.line(16,y,194,y);y+=8;doc.setFont('helvetica','bold');doc.text('CLIENTE',16,y);y+=6;doc.setFont('helvetica','normal');y=addText(doc,`Nome: ${sale.customer_name||'Venda balcão'}`,16,y);if(sale.customer_phone)y=addText(doc,`WhatsApp: ${sale.customer_phone}`,16,y);y=addText(doc,`Responsável pelo atendimento: ${sale.seller_name||'—'}`,16,y);y+=3;doc.setFont('helvetica','bold');doc.text('ITENS',16,y);y+=7;doc.setFillColor(246,248,251);doc.rect(16,y-5,178,8,'F');doc.text('Descrição',18,y);doc.text('Qtd.',128,y);doc.text('Unit.',151,y);doc.text('Total',192,y,{align:'right'});y+=8;doc.setFont('helvetica','normal');for(const i of sale.sale_items||[]){const lines=doc.splitTextToSize(String(i.description||'Item'),95);doc.text(lines,18,y);doc.text(String(i.quantity),130,y);doc.text(brl(i.unit_price),151,y);doc.text(brl(i.total),192,y,{align:'right'});y+=Math.max(7,lines.length*5);if(y>250){doc.addPage();y=24}}y+=4;doc.line(110,y,194,y);y+=7;doc.text(`Subtotal: ${brl(sale.subtotal)}`,194,y,{align:'right'});y+=6;if(Number(sale.discount||0)>0){doc.text(`Desconto: -${brl(sale.discount)}`,194,y,{align:'right'});y+=6}if(Number(meta.delivery_fee||0)>0){doc.text(`Entrega: ${brl(meta.delivery_fee)}`,194,y,{align:'right'});y+=6}doc.setFont('helvetica','bold');doc.setFontSize(12);doc.text(`TOTAL: ${brl(grand)}`,194,y,{align:'right'});y+=12;doc.setFontSize(9);doc.text('PRAZO E RECEBIMENTO',16,y);y+=6;doc.setFont('helvetica','normal');doc.text(`Previsão: ${meta.due_date?new Date(`${meta.due_date}T12:00:00`).toLocaleDateString('pt-BR'):'—'}${meta.due_time?` às ${meta.due_time}`:''}`,16,y);y+=6;doc.text(`Forma: ${fulfillmentName(meta.fulfillment)}`,16,y);y+=6;y=addText(doc,`${meta.fulfillment==='delivery'?'Endereço de entrega':'Local de retirada'}: ${deliveryAddress(meta,settings)}`,16,y);y+=3;doc.setFont('helvetica','bold');doc.text('PAGAMENTO',16,y);y+=6;doc.setFont('helvetica','normal');doc.text(`Forma: ${({pix:'Pix',dinheiro:'Dinheiro',debito:'Débito',credito:'Crédito',outro:'Outro'})[sale.payment_method]||sale.payment_method||'—'}`,16,y);y+=6;doc.text(`Condição: ${paymentConditionName(meta.payment_condition)}`,16,y);y+=6;doc.text(`Entrada recebida: ${brl(entry)}`,16,y);y+=6;doc.setFont('helvetica','bold');doc.text(`Saldo restante: ${brl(remaining)}`,16,y);y+=9;doc.setFont('helvetica','normal');if(remaining>0){const when=meta.fulfillment==='delivery'?'na entrega':'na retirada';doc.text(`Saldo de ${brl(remaining)} a pagar ${when} do material.`,16,y);y+=7}if(meta.note){doc.setFont('helvetica','bold');doc.text('OBSERVAÇÕES',16,y);y+=6;doc.setFont('helvetica','normal');y=addText(doc,meta.note,16,y)}y+=8;doc.setDrawColor(224,230,238);doc.line(16,y,194,y);y+=7;doc.setFontSize(8);const company=[companyAddress(settings),settings.whatsapp&&`WhatsApp ${settings.whatsapp}`,settings.phone&&`Tel. ${settings.phone}`,settings.email,settings.instagram,settings.website,settings.pix_key&&`PIX: ${settings.pix_key}`].filter(Boolean);for(const line of company)y=addText(doc,line,16,y,178);if(settings.receipt_footer){y+=2;doc.setFont('helvetica','bold');y=addText(doc,settings.receipt_footer,16,y,178)}return doc}
async function getSaleForReceipt(){if(lastEnhancedSale)return lastEnhancedSale;const sale=await latestSale();if(!sale)return null;lastEnhancedSale=sale;return sale}
function interceptReceipt(){const pdf=$('receiptPdfBtn'),wa=$('receiptWhatsappBtn');if(pdf&&!pdf.dataset.v6){pdf.dataset.v6='1';pdf.addEventListener('click',async e=>{e.preventDefault();e.stopImmediatePropagation();const sale=await getSaleForReceipt();if(!sale)return;const doc=await buildEnhancedReceipt(sale);doc.save(`REC-${String(sale.id).slice(0,8).toUpperCase()}.pdf`)},true)}if(wa&&!wa.dataset.v6){wa.dataset.v6='1';wa.addEventListener('click',async e=>{e.preventDefault();e.stopImmediatePropagation();const sale=await getSaleForReceipt();if(!sale)return;const meta=parseMeta(sale.note),grand=Number(sale.total||0)+Number(meta.delivery_fee||0),remaining=Math.max(0,Number(meta.remaining_amount||0)),date=meta.due_date?new Date(`${meta.due_date}T12:00:00`).toLocaleDateString('pt-BR'):'a combinar',text=`Olá, ${sale.customer_name||'cliente'}! Seu pedido na Papel e Código foi registrado.\n\nTotal: ${brl(grand)}\nEntrada: ${brl(meta.entry_amount||0)}\nSaldo: ${brl(remaining)}\nPrevisão: ${date}${meta.due_time?` às ${meta.due_time}`:''}\n${fulfillmentName(meta.fulfillment)}.${remaining?`\nSaldo restante a pagar ${meta.fulfillment==='delivery'?'na entrega':'na retirada'}.`:''}`;let d=normalizedPhone(sale.customer_phone);if(!d)return toast('WhatsApp não informado.',true);if(!d.startsWith('55'))d='55'+d;window.open(`https://wa.me/${d}?text=${encodeURIComponent(text)}`,'_blank','noopener')},true)}}

function monthRange(offset=0){const d=new Date();d.setMonth(d.getMonth()+offset,1);d.setHours(0,0,0,0);const y=d.getFullYear(),m=d.getMonth(),last=new Date(y,m+1,0).getDate(),pad=n=>String(n).padStart(2,'0');return{y,m,days:last,start:`${y}-${pad(m+1)}-01T00:00:00-03:00`,end:`${y}-${pad(m+1)}-${pad(last)}T23:59:59-03:00`}}
function cumulativeSeries(rows,days){const daily=Array(days).fill(0);for(const r of rows){const day=new Date(r.created_at).getDate()-1;if(day>=0&&day<days)daily[day]+=r.type==='entrada'?Number(r.amount||0):-Number(r.amount||0)}let sum=0;return daily.map(v=>sum+=v)}
function pathFor(values,w,h,min,max){if(!values.length)return'';const span=max-min||1,step=w/Math.max(1,values.length-1);return values.map((v,i)=>`${i?'L':'M'} ${(i*step).toFixed(1)} ${(h-(v-min)/span*h).toFixed(1)}`).join(' ')}
async function graphData(){const user=await currentUser();if(!user)return null;const cur=monthRange(0),prev=monthRange(-1),q=range=>supabase.from('cash_movements').select('type,amount,created_at').eq('user_id',user.id).gte('created_at',range.start).lte('created_at',range.end).order('created_at');const[a,b]=await Promise.all([q(cur),q(prev)]);const current=cumulativeSeries(a.data||[],cur.days),previous=cumulativeSeries(b.data||[],prev.days),today=Math.min(new Date().getDate(),cur.days),actual=current.slice(0,today),currentClose=actual.at(-1)||0,avg=currentClose/Math.max(1,today),projection=Array(cur.days).fill(null);for(let i=0;i<cur.days;i++)projection[i]=i<today?actual[i]:currentClose+avg*(i-today+1);return{cur,actual,projection,previous,currentClose,projectedClose:projection.at(-1)||0,previousClose:previous.at(-1)||0}}
function injectFinanceGraph(){const section=$('section-today');if(!section||$('todayFinanceGraph'))return;section.insertAdjacentHTML('afterbegin',`<article id="todayFinanceGraph" class="today-finance-graph"><div class="finance-graph-head"><div><span>FINANCEIRO DO MÊS</span><h3>Caixa e projeção</h3></div><div class="finance-graph-kpis"><div><small>Caixa do mês</small><b id="fgCurrent">R$ 0</b></div><div><small>Projeção</small><b id="fgProjection">R$ 0</b></div><div><small>Mês anterior</small><b id="fgPrevious">R$ 0</b></div></div></div><div class="finance-graph-canvas"><svg id="fgSvg" viewBox="0 0 1000 320" preserveAspectRatio="none" aria-label="Gráfico financeiro do mês"></svg></div><div class="finance-graph-legend"><span class="real">Caixa real</span><span class="projection">Projeção do mês</span><span class="previous">Mês anterior</span></div></article>`)}
async function renderFinanceGraph(){injectFinanceGraph();if(!$('todayFinanceGraph'))return;try{const d=await graphData();if(!d)return;$('fgCurrent').textContent=brl(d.currentClose);$('fgProjection').textContent=brl(d.projectedClose);$('fgPrevious').textContent=brl(d.previousClose);const all=[...d.actual,...d.projection,...d.previous,0].filter(Number.isFinite),min=Math.min(...all),max=Math.max(...all),w=1000,h=250,padTop=25,padBottom=35,plotH=h-padTop;const actualFull=Array(d.cur.days).fill(null);d.actual.forEach((v,i)=>actualFull[i]=v);const pActual=pathFor(d.actual,w,plotH,min,max),pProj=pathFor(d.projection,w,plotH,min,max),pPrev=pathFor(d.previous,w,plotH,min,max);const zeroY=(plotH-(0-min)/(max-min||1)*plotH).toFixed(1),mid=Math.ceil(d.cur.days/2),last=d.cur.days;$('fgSvg').innerHTML=`<g transform="translate(0 ${padTop})"><line x1="0" y1="${zeroY}" x2="1000" y2="${zeroY}" class="fg-zero"/><path d="${pPrev}" class="fg-line fg-prev"/><path d="${pProj}" class="fg-line fg-proj"/><path d="${pActual}" class="fg-line fg-real"/></g><g class="fg-days"><text x="0" y="305">1</text><text x="500" y="305" text-anchor="middle">${mid}</text><text x="1000" y="305" text-anchor="end">${last}</text></g>`}catch(e){console.warn('Gráfico financeiro V6',e)}}

function setup(){injectCss();const timer=setInterval(()=>{injectSaleFields();interceptReceipt();if(!wrapped)wrapFinish();injectFinanceGraph();if($('todayFinanceGraph')){clearInterval(timer);renderFinanceGraph();graphTimer=setInterval(()=>{if(document.visibilityState==='visible')renderFinanceGraph()},60000)}},100);setTimeout(()=>clearInterval(timer),15000);const obs=new MutationObserver(()=>{if(!wrapped)wrapFinish();injectSaleFields();injectFinanceGraph();interceptReceipt()});obs.observe(document.body,{subtree:true,childList:true})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',setup);else setup();
